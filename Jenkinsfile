pipeline {
    agent any

    stages {
        stage ('Print') {
            steps {
                bat "python updateAPI.py" 
    stages {
        stage ('install') {
            steps {
                bat "pip install flask_cors"
               } 
            }
        }
    }
}
